# MySQL Installation
- Homebrew
```ssh
> brew install mysql
> mysql.server start
```
- Yum
```ssh
sudo yum install mysql-server
sudo /sbin/service mysqld start

#answer Yes to all
sudo /usr/bin/mysql_secure_installation
```
